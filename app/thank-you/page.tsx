'use client'; import Link from 'next/link'; import { useEffect,useState } from 'react'; import { money } from '@/lib/product';
type Order={orderId:string;productName:string;quantity:number;totalPrice:number}; export default function ThankYou(){const [order,setOrder]=useState<Order|null>(null);useEffect(()=>{const saved=sessionStorage.getItem('lastOrder');if(saved)setOrder(JSON.parse(saved))},[]);return <main className="grid min-h-screen place-items-center bg-[#eef0e5] p-5"><section className="card w-full max-w-xl p-8 text-center sm:p-14"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-lime text-3xl">✓</div><p className="eyebrow mt-7">Order received</p><h1 className="mt-2 text-4xl font-black">Thank you for your order!</h1><p className="mt-5 leading-7 text-black/65">Our sales representative will call you soon to confirm your order.</p>{order&&<div className="mt-8 rounded-2xl bg-[#f4f5ed] p-5 text-left text-sm"><p className="font-bold">Order ID: {order.orderId}</p><div className="mt-4 flex justify-between"><span>{order.productName}</span><b>× {order.quantity}</b></div><div className="mt-3 flex justify-between border-t border-black/10 pt-3 font-black"><span>Total price</span><span>{money(order.totalPrice)}</span></div><p className="mt-4 text-black/60">Payment method: Cash On Delivery</p></div>}<Link href="/" className="btn-dark mt-8">Back to Home</Link></section></main>}

'use client';
import { useSearchParams,useRouter } from 'next/navigation'; import { FormEvent,useState } from 'react'; import { money,product } from '@/lib/product';
export default function Checkout(){const p=useSearchParams(),router=useRouter();const [qty,setQty]=useState(Math.max(1,Number(p.get('quantity'))||1)),[zone,setZone]=useState('kathmandu'),[loading,setLoading]=useState(false),[error,setError]=useState('');const delivery=zone==='kathmandu'?0:100,total=qty*product.offerPrice+delivery; async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const form=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch('/api/order',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,quantity:qty,pricePerPiece:product.offerPrice,totalPrice:total,deliveryFee:delivery})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Could not submit your order.');sessionStorage.setItem('lastOrder',JSON.stringify(data.order));router.push('/thank-you')}catch(err){setError(err instanceof Error?err.message:'Something went wrong.');setLoading(false)}}return <main className="min-h-screen bg-[#eef0e5] py-10"><div className="shell grid max-w-5xl gap-8 lg:grid-cols-[1fr_.75fr]"><form onSubmit={submit} className="card p-6 sm:p-9"><p className="eyebrow">Secure COD checkout</p><h1 className="mt-2 text-4xl font-black">Complete your order</h1><p className="mt-3 text-black/60">No payment needed now. We&apos;ll call to confirm your order.</p><div className="mt-7 grid gap-4 sm:grid-cols-2"><Field label="Full Name" name="name"/><Field label="Phone Number" name="phone" type="tel"/><Field label="Email Address" name="email" type="email"/><label className="text-sm font-bold">Delivery area<select name="deliveryZone" value={zone} onChange={e=>setZone(e.target.value)} className="mt-2 w-full rounded-xl border border-black/15 bg-white p-3 font-normal"><option value="kathmandu">Inside Kathmandu Valley — Free</option><option value="outside">Outside Kathmandu Valley — NPR 100</option></select></label></div><label className="mt-4 block text-sm font-bold">Exact Location<textarea required name="location" placeholder="Kindly share your exact location" className="mt-2 min-h-24 w-full rounded-xl border border-black/15 p-3 font-normal"/></label><input type="hidden" name="productName" value={product.name}/>{error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-sm font-medium text-red-700">{error}</p>}<button disabled={loading} className="btn-primary mt-6 w-full disabled:opacity-60">{loading?'Submitting Order...':'Order Now — Cash On Delivery'}</button></form><aside className="h-fit rounded-3xl bg-ink p-7 text-white"><p className="text-xs font-bold uppercase tracking-widest text-lime">Your order</p><h2 className="mt-3 text-2xl font-black">{product.name}</h2><div className="mt-6 space-y-4 text-sm text-white/75"><label className="block font-bold text-white">Quantity<div className="mt-2 flex w-fit items-center rounded-full bg-white/10"><button type="button" onClick={()=>setQty(Math.max(1,qty-1))} className="px-4 py-2">−</button><span className="px-2 text-white">{qty}</span><button type="button" onClick={()=>setQty(qty+1)} className="px-4 py-2">+</button></div></label><Row title="Price per piece" value={money(product.offerPrice)}/><Row title="Delivery" value={delivery?'NPR 100':'Free'}/><div className="border-t border-white/15 pt-4"><Row title="Total price" value={money(total)} strong/></div></div><p className="mt-7 border-t border-white/15 pt-5 text-sm text-white/60">✓ Cash on Delivery &nbsp; ✓ Support available &nbsp; ✓ Fast delivery</p></aside></div></main>}
function Field({label,name,type='text'}:{label:string;name:string;type?:string}){return <label className="text-sm font-bold">{label}<input required name={name} type={type} className="mt-2 w-full rounded-xl border border-black/15 p-3 font-normal"/></label>} function Row({title,value,strong}:{title:string;value:string;strong?:boolean}){return <div className={strong?'flex justify-between text-lg font-black text-white':'flex justify-between'}><span>{title}</span><span>{value}</span></div>}

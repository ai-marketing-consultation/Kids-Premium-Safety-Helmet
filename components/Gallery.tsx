'use client';
import Image from 'next/image'; import { useState } from 'react';
export function Gallery({ images }: { images: string[] }) { const [active,setActive]=useState(0); return <div><div className="relative aspect-square overflow-hidden rounded-[2rem] bg-[#eef0e5]"><Image src={images[active]} alt="Kids helmet" fill className="object-contain p-4" priority /><button aria-label="Previous image" onClick={()=>setActive((active+images.length-1)%images.length)} className="absolute left-3 top-1/2 rounded-full bg-white/90 px-3 py-2">←</button><button aria-label="Next image" onClick={()=>setActive((active+1)%images.length)} className="absolute right-3 top-1/2 rounded-full bg-white/90 px-3 py-2">→</button></div><div className="mt-3 flex gap-2 overflow-x-auto pb-1">{images.map((src,i)=><button key={src} aria-label={`View image ${i+1}`} onClick={()=>setActive(i)} className={`relative h-16 w-16 shrink-0 overflow-hidden rounded-xl border-2 ${i===active?'border-lime':'border-transparent'}`}><Image src={src} alt="Helmet detail" fill className="object-cover" /></button>)}</div></div> }
